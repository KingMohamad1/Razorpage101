@page
@using SailClubLibrary.Models
@model RazorBoatApp2026InClass.Pages.Members.IndexModel
@{
    ViewData["Title"] = "List members";
}

<h1>List of members</h1>

<p>
    <a asp-page="CreateBoat">Create a new member</a>
</p>

<table class="table">
    <thead>
        <tr>
            <th>ID</th>
            <th>FirstName</th>
            <th>SurName</th>
            <th>Mail</th>
            <th>PhoneNumber</th>
            <th>City</th>
            <th>Address</th>
            <th>TheMemberRole</th>
            <th>TheMemberType</th>
        </tr>
    </thead>
    <tbody>
        @foreach (Member item in Model.Members)
        {
            <tr>
                <td>@item.Id</td>
                <td>@item.FirstName</td>
                <td>@item.SurName</td>
                <td>@item.Mail</td>
                <td>@item.PhoneNumber</td>
                <td>@item.City</td>
                <td>@item.Address</td>
                <td>@item.TheMemberRole</td>
                <td>@item.TheMemberType</td>
            </tr>
        }

    </tbody>
</table>